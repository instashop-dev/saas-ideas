node_modules/
dist/
.env
.env.local
*.log
.DS_Store

# Pipeline artifacts ARE committed: the Git-native state machine relies on each stage
# committing + pushing its outputs so downstream stages and Recovery can consume them
# from a fresh checkout. Directory placeholders (.gitkeep) remain tracked on purpose.

.reasonix/
