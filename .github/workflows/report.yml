name: Report

on:
  workflow_dispatch:
    inputs:
      run_id:
        description: 'Run ID to process'
        required: true
        type: string
  workflow_run:
    workflows: ['Judge']
    types:
      - completed

permissions:
  contents: write
  issues: write

concurrency:
  group: report-${{ github.run_id }}
  cancel-in-progress: false

jobs:
  report:
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - run: npm ci

      - name: Determine Run ID
        id: run-id
        run: |
          if [ -n "${{ inputs.run_id }}" ]; then
            echo "RUN_ID=${{ inputs.run_id }}" >> $GITHUB_OUTPUT
          else
            LATEST=$(ls -t research/runs/RUN-*.json 2>/dev/null | head -1)
            if [ -n "$LATEST" ]; then
              RUN_ID=$(basename "$LATEST" .json)
              echo "RUN_ID=$RUN_ID" >> $GITHUB_OUTPUT
            else
              echo "No run manifests found"
              exit 1
            fi
          fi

      - name: Generate Reports
        run: |
          npx tsx src/cli/stage-report.ts "${{ steps.run-id.outputs.RUN_ID }}"

      - name: Commit and Push Reports
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add reports/ research/runs/
          git diff --staged --quiet || git commit -m "[${{ steps.run-id.outputs.RUN_ID }}] Final reports"
          git push
